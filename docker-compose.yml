services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-}
        VITE_APP_ENV: ${VITE_APP_ENV:-}
    env_file:
      - .env
    ports:
      - "5174:80"
    restart: unless-stopped
